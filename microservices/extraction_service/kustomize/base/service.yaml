apiVersion: v1
kind: Service
metadata:
  name: extraction-service
  labels:
    app: extraction-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: extraction-service
