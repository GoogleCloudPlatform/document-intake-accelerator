apiVersion: v1
kind: Service
metadata:
  name: document-status-service
  labels:
    app: document-status-service
spec:
  type: NodePort
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: document-status-service