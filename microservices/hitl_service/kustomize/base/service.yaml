apiVersion: v1
kind: Service
metadata:
  name: hitl-service
  labels:
    app: hitl-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: hitl-service
