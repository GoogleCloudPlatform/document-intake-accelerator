apiVersion: v1
kind: Service
metadata:
  name: hitl-service
  labels:
    app: hitl-service
spec:
  type: NodePort
    #loadBalancerIP: 35.224.197.56
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: hitl-service
